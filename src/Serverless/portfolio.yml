service: LearningPortfolio

# https://www.serverless.com/framework/docs/providers/aws/events/apigateway/#example-lambda-proxy-event-default

#provider: aws
provider:
  name: aws
  runtime: nodejs14.x  # explicit to avoid warning
  stage: test
  region: eu-central-1
  lambdaHashingVersion: "20201221" # optimize resolution of lambda function (and avoid warning)
  #deploymentBucket:
  #  name: ${file(./shared-variables.yml):deploymentBucket.name}
#package:
#  individually: true

functions:
  portfolio: # any name
    handler: Learning::Learning.Portfolio.CreateFund::Handle   # assembly::namespace.class::method
    #package:
    #  artifact: ${file(shared-variables.yml):zipFile}
    #warmup: true
    #alarms:
    #   - functionErrors
    #   - functionDuration
    events:
      # - http: GET fund
      - http: 
          path: learning/portfolio/fund
          method: POST
        # cors: true
        #authorizer:
        #  type: "COGNITO_USER_POOLS"
        #  scopes: 
        #    - ...
        #    - ... 
        #  authorizedId:
        #    Fn::ImportValue: "..."


    #<file src="client-data-collection\serverless.yml" target="functions\client-data-collection"/>
    #<file src="Services\ClientDataCollection.Lambda\bin\Release\netcoreapp3.1\clientdatacollectiondomain-deploy-package.zip" target="functions" />
